Re-forms
==========

Simple resubmittable forms with unique URLs and admin panels

Local Development:

```sh
mkdir db
npm install
npm run mongo
npm start
```

Config:

```sh
cp -r config.template config
```
